const SectionLabel = ({ label }) => {
  return (
    <div className="mb-2 mt-4 border-b border-slate-100 pb-1.5 text-xs font-semibold uppercase tracking-wider text-slate-500 first:mt-0">
      {label}
    </div>
  );
};

export default SectionLabel;
